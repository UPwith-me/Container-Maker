# 🚀 Full-Stack AI Platform Demo

> **由 Container-Maker 驱动的现代 AI 开发环境**

这是一个展示 Container-Maker 全部能力的复杂项目示例，包含：

## 🏗️ 项目架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Container-Maker                          │
│                    (一键启动整个环境)                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │   React     │  │   FastAPI   │  │   PyTorch   │        │
│  │  Frontend   │  │   Backend   │  │   ML/AI     │        │
│  │   :3000     │  │   :8000     │  │   GPU       │        │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘        │
│         │                │                │                │
│  ┌──────┴────────────────┴────────────────┴──────┐        │
│  │              Docker Compose Network            │        │
│  └──────┬────────────────┬────────────────┬──────┘        │
│         │                │                │                │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐        │
│  │ PostgreSQL  │  │    Redis    │  │   Qdrant    │        │
│  │    :5432    │  │    :6379    │  │   :6333     │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐                          │
│  │  Prometheus │  │   Grafana   │                          │
│  │    :9090    │  │    :3001    │                          │
│  └─────────────┘  └─────────────┘                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 一键启动

```bash
# 克隆项目并进入目录
cd demo-fullstack-ai

# 使用 Container-Maker 启动
cm shell
```

**就这样！** Container-Maker 会自动：
1. 解析 `devcontainer.json` 配置
2. 启动 Docker Compose (6 个服务)
3. 安装 5 个 DevContainer Features
4. 配置 GPU 支持
5. 转发 7 个端口
6. 执行初始化脚本
7. 进入开发环境

## 📋 功能展示

### DevContainer Features (自动安装)
- ✅ Python 3.11 + pip
- ✅ Node.js 20 + pnpm
- ✅ Docker-in-Docker
- ✅ GitHub CLI
- ✅ Common Utils (zsh, htop, vim)

### 服务架构 (Docker Compose)
- ✅ PostgreSQL 16 - 主数据库
- ✅ Redis 7 - 缓存和消息队列
- ✅ Qdrant - AI 向量数据库
- ✅ Prometheus - 监控
- ✅ Grafana - 可视化仪表盘

### GPU 支持
- ✅ NVIDIA CUDA 12.2
- ✅ PyTorch with GPU
- ✅ Transformers / LangChain

## 💻 开发命令

```bash
# 启动开发服务器
make dev

# 运行测试
make test

# 训练 ML 模型
make train

# 数据库迁移
make migrate
```

## 🛠️ Container-Maker 使用的功能

| 功能 | devcontainer.json 配置 |
|------|------------------------|
| Docker Compose | `"dockerComposeFile": "docker-compose.yml"` |
| Features | `"features": { ... }` |
| GPU | `"runArgs": ["--gpus", "all"]` |
| 端口转发 | `"forwardPorts": [3000, 8000, ...]` |
| 环境变量 | `"containerEnv": { ... }` |
| 生命周期钩子 | `"postCreateCommand": "..."` |
| 挂载 | `"mounts": [...]` |

---

**这就是 Container-Maker 的力量 —— 一个配置文件，一条命令，完整的开发环境！** 🎉
